package atd.f0;

import atd.i0.f;
import atd.s0.a;

public final class d extends f {
    /* JADX INFO: super call moved to the top of the method (can break code semantics) */
    d(byte[] bArr) {
        super(bArr == null ? new byte[0] : bArr);
    }

    /* JADX INFO: super call moved to the top of the method (can break code semantics) */
    d(String str) {
        super(str == null ? a.a(-798587703061056L) : str);
    }
}
